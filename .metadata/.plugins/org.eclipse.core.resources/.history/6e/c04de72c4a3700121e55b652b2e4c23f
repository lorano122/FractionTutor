package helpers;

import android.graphics.Bitmap;
import android.graphics.Canvas;

public class Location {
	
	private Bitmap icon;
	private float x,y;
	private int index;
	
	public Location(Bitmap icon, int index)
	{
		this.icon = icon;
		this.index = index;
	}
	
	//sets the location based on index, index maps the icon to its text
	public void setLocation(int width, int height)
	{
		float tempx,tempy;
		if(index == 1)
		{
			tempx = 50;
			tempy = (height/2) - icon.getHeight();
		}
		else if(index == 2)
		{
			tempx = (width/2) - (icon.getWidth()/2);
			tempy = 50;
		}
		else if(index ==3)
		{
			tempx = (width/2) - (icon.getWidth()/2);
			tempy = height - icon.getHeight();
		}
		else
		{
			tempx = width - 50;
			tempy = (height/2) - icon.getHeight();
		}
		x = tempx;
		y=tempy;
	}
	
	public void draw(Canvas canvas)
	{
		setLocation(canvas.getWidth(),canvas.getHeight());
		canvas.drawBitmap(icon, x, y,null);
	}

}
