package mathClasses;

public class Square {

	private boolean colored;
	private int x,y,size;
	
	public Square(boolean colored, int x, int y, int size)
	{
		this.colored = colored;
		this.x=x;
		this.y = y;
		this.size = size;
	}
}
